import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { BlockingNotice } from "@/components/evidence";
const pages:Record<string,{title:string;intro:string;claim:string}>={"notas-en-siu":{title:"Falta una nota en SIU",intro:"No des por cerrado el recorrido académico hasta que el registro sea correcto.",claim:"DIP-01"},equivalencias:{title:"Equivalencias e inclusiones pendientes",intro:"Identificá si todavía necesitás una resolución numerada antes del diploma.",claim:"INC-04"},diploma:{title:"No puedo iniciar el diploma",intro:"Revisá primero los requisitos que bloquean el inicio seguro del trámite.",claim:"DIP-02"}};
export function generateStaticParams(){return Object.keys(pages).map(slug=>({slug}))} export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;return {title:pages[slug]?.title??"Problema"}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const page=pages[slug];if(!page)notFound();return <div className="container page"><p className="eyebrow">Resolver un problema</p><h1>{page.title}</h1><p className="lead">{page.intro}</p><BlockingNotice claimId={page.claim}/></div>}
