import type { Metadata } from "next"; import { loadRegistry } from "@/lib/knowledge";
export const metadata:Metadata={title:"Fuentes"}; export default function Page(){const registry=loadRegistry(); return <div className="container page"><p className="eyebrow">Confianza y trazabilidad</p><h1>Fuentes</h1><p className="lead">Cada requisito operativo apunta a un registro central. “Revisada” indica la verificación editorial de esta versión, no una actualización en tiempo real.</p><div className="grid">{Object.entries(registry.sources).filter(([,s])=>s.url).map(([id,s])=><article className="card" key={id}><h3>{s.title}</h3><p>{s.authority}</p><p className="meta">Fuente revisada para esta versión: 11 ago 2026.</p><a className="source" href={s.url} target="_blank" rel="noreferrer">Abrir fuente ↗</a></article>)}</div></div>}
