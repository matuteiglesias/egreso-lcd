import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { Evidence } from "@/components/evidence";
const pages:Record<string,{title:string;intro:string;claim:string}>={"tutor-y-pei":{title:"Tutor/a y PEI",intro:"Ordená la aprobación de tutoría y el plan de materias antes de avanzar.",claim:"PEI-02"},tesis:{title:"Tesis",intro:"Verificá las condiciones formales para comenzar y seguí el circuito de evaluación.",claim:"THS-02"},"cierre-academico":{title:"Cierre académico",intro:"Antes del diploma, comprobá que materias y tesis estén registradas correctamente.",claim:"DIP-01"},diploma:{title:"Diploma",intro:"Reuní los requisitos académicos y documentales antes de iniciar el trámite.",claim:"DIP-03"},"jura-y-entrega":{title:"Jura y entrega",intro:"Cuando el diploma esté listo, revisá las opciones oficiales para recibirlo.",claim:"JUR-01"}};
export function generateStaticParams(){return Object.keys(pages).map(slug=>({slug}))} export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;return {title:pages[slug]?.title??"Etapa"}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const page=pages[slug];if(!page)notFound();return <div className="container page"><p className="eyebrow">Etapa del recorrido</p><h1>{page.title}</h1><p className="lead">{page.intro}</p><Evidence claimId={page.claim}/></div>}
